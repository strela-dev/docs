import { Libraries } from '@/components/Libraries'

export const metadata = {
    title: 'Strela SDKs',
    description:
        'Strela offers fine-tuned Kotlin, Java and Go libraries to make your life easier and give you the best experience when using Strela.',
}

export const sections = [
    { title: 'Official libraries', id: 'official-libraries' },
]

# Protocol SDKs

The recommended way to interact with the Strela API is by using one of our official SDKs. Today, Strela offers fine-tuned Kotlin, Java, and Go libraries to make your life easier and give you the best experience when using Strela. {{ className: 'lead' }}

<Libraries />
